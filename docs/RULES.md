# 규칙

## 금지

1. **비공개 Test 결과를 보고 다시 학습하지 않습니다.**
   Test가 모델 선택에 쓰이면 더 이상 독립된 평가가 아닙니다.
   모델은 Public Validation으로만 고릅니다.

2. **`private_test` 이미지는 배포되지 않습니다.** 어떤 경로로든 입수해
   학습이나 검증에 사용하지 않습니다.

3. **분할을 임의로 다시 나누지 않습니다.**
   `train` 과 `public_val` 을 합쳐 학습하거나, experiment_id를 무시하고
   무작위로 재분할하면 데이터 누수가 생깁니다.

4. **외부 데이터 추가 금지.** 제공된 데이터만 사용합니다.

5. **모델은 ResNet18로 고정합니다.** 이번 과제의 목표는
   모델 크기 경쟁이 아니라 실험 설계이며, 팀 간 비교를 공정하게 유지해야 합니다.

## 허용

- torchvision ImageNet 사전학습 가중치 사용
- `train_baseline.py` 의 모든 CLI 옵션
- 전처리·증강 코드 수정 (`common.py` 의 `get_transforms`)
- 여러 체크포인트 중 하나를 최종으로 선택

## Colab 사용

- 개인 Colab 계정으로 각자 실행합니다. 팀별로 GPU가 분리되므로 서로 간섭하지 않습니다.
- 무료 GPU는 **보장이 아니라 배정** 입니다. 마감 직전에 몰리면 GPU를 못 받을 수 있습니다.
  **마감 3일 전까지 학습을 끝내는 것을 권장합니다.**
- 무료 GPU 한도를 다 썼거나 학습이 도저히 안 돌아가면 운영진에게 알려주세요.
  Colab Pro 또는 별도 GPU 환경으로 대신 실행해 드립니다.
- 세션이 끊기면 `/content/` 안의 파일이 사라집니다. `best_model.pt` 는
  꼭 자기 드라이브나 로컬로 내려받아 보관하세요.
